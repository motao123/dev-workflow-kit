# Environment Config Audit Examples

- "Use environment-config-audit to identify config drift before rollout."
- "Use environment-config-audit to see whether this failure is more likely secrets/config than code."
- "Use environment-config-audit to check if feature-flag assumptions are safe across environments."
